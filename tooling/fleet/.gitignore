# Real inventory carries tailnet addresses; the repo is public.
inventory.local.yml
*.retry

# Encrypted vault of per-host sudo passwords - stays LOCAL only (public repo).
# Even encrypted, production sudo passwords do not belong in a public repo.
group_vars/*/vault.yml
