### Mascope production package build instructions

This module contains scripts for building mascope bundle packages (frontend, backend, fileservices...)

Build server is Windows-powered host with access to source code and development environment.

Relevant build.cmd creates the bundle package, which should then be published (via communication directory or build repository...).
